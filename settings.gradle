rootProject.name = 'gradle-cookbook'
include 'common', 'library', 'data', 'service', 'ui', 'app'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            // libs.versions.toml is picked up automatically
        }
    }
}
